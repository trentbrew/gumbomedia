# Website V2

This project is set up to be hosted on Vercel.

## Structure

- `page.html`: The main entry point of the website.
- `vercel.json`: Configuration to serve `page.html` as the root (`/`).

## Deployment

When deploying to Vercel, simply import this repository. The `vercel.json` file will automatically handle the routing.
